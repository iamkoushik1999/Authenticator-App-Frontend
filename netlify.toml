[build]
  base = "authenticator-app-frontend"
  command = "npm run build"
  publish = "dist"

# Let React Router handle direct visits and browser refreshes on client-side routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
